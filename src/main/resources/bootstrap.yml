spring:
  application:
    name: refresh-issue
  cloud:
    config:
      enabled: true

eureka:
   client:
      enabled: true
      registerWithEureka: true
      fetchRegistry: true
#      serviceUrl:
#         defaultZone: http://localhost:8761/eureka/